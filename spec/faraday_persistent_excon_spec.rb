require 'spec_helper'

describe FaradayPersistentExcon do
  it 'has a version number' do
    expect(FaradayPersistentExcon::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
